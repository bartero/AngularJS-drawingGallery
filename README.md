# angular-drawing

```
$scope.colors = [];
for(let i = 0; i < 0; i++) {
   let color = [randomInt(0, 255), randomInt(0, 255), randomInt(0, 255)];
   $scope.colors.push(color);
}
```

```javascript
$scope.colors = [];
for(let i = 0; i < 0; i++) {
   let color = [randomInt(0, 255), randomInt(0, 255), randomInt(0, 255)];
   $scope.colors.push(color);
}
```

```js
$scope.colors = [];
for(let i = 0; i < 0; i++) {
   let color = [randomInt(0, 255), randomInt(0, 255), randomInt(0, 255)];
   $scope.colors.push(color);
}
```
